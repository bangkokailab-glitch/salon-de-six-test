# ZIPLUS LP client preview

Client-facing GitHub Pages preview for three first-view directions:

- `/a/` — balanced company-benefit direction
- `/c/` — document-conversion and authority direction
- `/d/` — cinematic action-dock direction

This proposal preview is PC-only. Smartphone-specific design and implementation are out of scope.

All CTA links are proposal-stage placeholders (`#document-request` and `#education-video`). Replace them before production use.

The ten-second background motion files are provisional animations created from the current approved photo. Replace them with the supplied promotional video during production.

The site includes `noindex` metadata and `robots.txt`, but a public GitHub Pages site is still accessible to anyone who knows the URL.
